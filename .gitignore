# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Test / tooling
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
node_modules/

# Models (auto-downloaded by ultralytics on first run; *.onnx is exported by
# eval/export_onnx.py, not committed the same way *.pt is not)
*.pt
*.onnx

# Uploaded / working media
*.mp4
*.mov
*.avi
!tests/**/*.mp4

# Validation data (keep the example + README, ignore real labels/clips/traces —
# a trace is coordinates rather than footage, so unlike clips it's safe to
# commit deliberately as a fixture; the bulk working directory still isn't)
data/labels.csv
data/clips/
data/traces/

# Editor / OS
.DS_Store
.idea/
.vscode/
